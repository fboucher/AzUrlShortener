# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

# azure.yaml
name: AzUrlShortener
services:
  azfunc-light:
    project: ../FunctionsLight/Cloud5mins.ShortenerTools.FunctionsLight.csproj
    language: dotnet
    host: function # <-- Tells AZD this is a native Azure Function App!

  # App Service for Api + Admin (Main + Sidecar or Multi-container)
  admin:
    project: ../TinyBlazorAdmin/Cloud5mins.ShortenerTools.TinyBlazorAdmin.csproj
    language: dotnet
    host: appservice