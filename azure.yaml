# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: az-url-shortener
services:  
  app:
    language: dotnet
    project: ./src/AppHost/Cloud5mins.ShortenerTools.AppHost.csproj
    host: containerapp
